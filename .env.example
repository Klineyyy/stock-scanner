# Leave both empty to run without a database (the "Demo" backend keeps data in the browser).
# Fill both in to enable the Supabase backend: Project Settings > API.
VITE_SUPABASE_URL=
VITE_SUPABASE_PUBLISHABLE_KEY=
